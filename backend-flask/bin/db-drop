#! /usr/bin/bash

echo "db-drop"

# sed could be used to replace partial text.
NO_DB_CONNECTION_URL=$(sed 's/\/cruddur//g' <<< "$CONNECTION_URL")
psql $NO_DB_CONNECTION_URL -c "drop database cruddur;"